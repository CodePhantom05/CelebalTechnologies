# Core RAG dependencies
pymupdf==1.24.14            # PDF text extraction (handles unusual font encodings better than pypdf)
sentence-transformers==3.3.1  # Embedding model (all-MiniLM-L6-v2)
faiss-cpu==1.9.0            # Vector similarity search
transformers==4.47.1        # Generation model (Flan-T5)
torch==2.5.1                # Backend for transformers/sentence-transformers
rank-bm25==0.2.2            # Keyword search for hybrid retrieval
numpy==1.26.4
datasets==3.2.0             # Only needed if using the Hugging Face dataset option
streamlit==1.40.2           # Web UI (run with: streamlit run streamlit_app.py)
cohere==5.13.3              # Only needed if using generation_backend="cohere" (command-r)
python-dotenv==1.0.1        # Loads COHERE_API_KEY from a local .env file in streamlit_app.py